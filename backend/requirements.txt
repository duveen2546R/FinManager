# Web framework + API security
Flask==3.1.2
gunicorn==23.0.0  # production WSGI server (Render start command)
Flask-Cors==6.0.1
Flask-Bcrypt==1.0.1
Flask-JWT-Extended==4.7.1
Flask-Limiter==3.12

# PostgreSQL
psycopg2-binary==2.9.10

# Config
python-dotenv==1.1.1

# Optional LLM chat answers (app.py lazily imports ChatGroq when
# GROQ_API_KEY is set; the app runs fine without a key).
langchain-groq

# Tests (backend/tests, run with: python3 -m pytest backend/tests -q)
pytest
